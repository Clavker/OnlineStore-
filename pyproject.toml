[tool.poetry]
name = "onlinestore"
version = "0.1.0"
description = ""
authors = ["Clavker <Clavker.Fust@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^6.0.2"
django-admin-interface = "^0.32.0"
psycopg2-binary = "^2.9.11"
python-dotenv = "^1.2.2"
pillow = "^12.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "flake8 (>=7.3.0,<8.0.0)"
]
